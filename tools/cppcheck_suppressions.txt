# =====================================================================
# cppcheck 억제 목록 (rpi: 커널 드라이버 + 데몬)
#   형식: id[:파일글롭[:라인]]
# =====================================================================

# 커널 헤더(linux/*)는 호스트에 없어 미탐 → 억제
missingInclude
missingIncludeSystem

# 커널 file_operations 콜백(open/read/ioctl 등)은 구조체에 함수포인터로만
# 등록돼 cppcheck 엔 "미사용 함수"로 보임 → 억제
unusedFunction

# 벤더/시스템 경로 (있으면)
*:*/linux/*
